
import UIKit
import Combine

class NavbadCoordinator {

    var rootController: UINavigationBar

    init(rootController: UINavigationBar) {
        self.rootController = rootController
        rootController.tintColor = .white
        rootController.backgroundColor = UIColor(named: "Red")
    }

    func start() {
        
    }

    func navbar() {

    }
}
